{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the clipboard manager windows.",
  "windows": ["main", "image-*"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
